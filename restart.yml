---
# tasks to restart the server

- name: restart server
  command: /sbin/shutdown -r
  async: 0
  poll: 0
  ignore_errors: true

- name: wait for the server to come back
  local_action: wait_for host=wp-dev state=started delay=30 timeout=600
  become: no
